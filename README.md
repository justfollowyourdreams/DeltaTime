# DeltaTime

An example of coding animation unlinked to frames but instead to time, so game won't slow down on a framerate fall.

Example is [here](Main.cpp). Other files are meant to make it runnable (on Windows with [MSYS2 MinGW](https://packages.msys2.org/packages/mingw-w64-x86_64-gcc)).